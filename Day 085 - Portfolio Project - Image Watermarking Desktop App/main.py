from tkinter import Tk
from watermarkapp import WatermarkApp

root = Tk()  # Create the main application window
app = WatermarkApp(root)  # Create a Watermark App by passing the window
root.mainloop()  # Start the main loop of the window